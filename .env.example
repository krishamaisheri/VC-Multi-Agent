# ============================================
# VC Multi-Agent System Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git

# ============================================
# Gemini (Gemma) Configuration
# ============================================
# Get your API key from: https://aistudio.google.com/
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini model to use for LLM calls (must support generateContent)
GEMINI_MODEL=gemini-2.5-flash

# ============================================
# Chroma Vector Database Configuration
# ============================================
# Chroma runs embedded, persisted to a local directory - no server required.
CHROMA_PERSIST_DIR=./data/chroma_storage

# Collection name for storing pitches
CHROMA_COLLECTION=vc_pitches

# ============================================
# Voice Processing Configuration
# ============================================
SPEECH_RECOGNITION_LANGUAGE=en-US

# ============================================
# Server Configuration
# ============================================
DEBUG=False
HOST=0.0.0.0
PORT=8000

# ============================================
# Instructions
# ============================================
# 1. Gemini API Key:
#    - Sign up at https://aistudio.google.com/
#    - Generate an API key from settings
#    - Paste it above as GEMINI_API_KEY
#
# 2. Chroma Database:
#    - No setup needed - Chroma persists to CHROMA_PERSIST_DIR on disk.
#    - Delete that directory to reset stored embeddings.
#
# 3. Keep this file SECRET:
#    - Never commit .env to git (covered by .gitignore)
#    - Don't share your API keys
#    - Rotate keys regularly
